function PLRS:rblx()
  self:enabledModules(<inf)
  local self:PLRS = PLRS:enabledModules(<inf,"anim")
  local Enum.PLRS = begin; dbg(self,i); if i= or >512 print("A limited variable exceeds or is definied as 512. ERROR: 514")
    global PLRS.DataModel = 0
    for i < 512, #subScores do 
      function appendChild()
         global testChild = PLRS + i
         if testChild <i
          print("Negative limited variable. ERROR: 001")
         end
        end
      end
    end
  end
end
